import React from 'react'

function Add()
{
   
    direction = directions.map(direction => direction);
    document.write(directions:String[]);
    return(
    <div>
         <button onClick={() => handleClick()}>Submit</button>
    </div>
    );
    };
export default Add;